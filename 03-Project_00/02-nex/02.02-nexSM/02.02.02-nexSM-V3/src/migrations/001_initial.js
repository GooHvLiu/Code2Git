/**
 * ============================================================
 * 初始迁移（基线）
 * ------------------------------------------------------------
 * 功能描述：数据库表结构在迁移系统引入之前已经存在，本迁移不执行任何 DDL，
 *           仅作为基线标记（baseline）写入 schema_migrations 表，
 *           表示当前库结构即为迁移起点，后续变更从 002 开始管理。
 *           up() / down() 均为空操作，不修改任何现有表结构或数据。
 * ------------------------------------------------------------
 * 作者：GooHv
 * 创建日期：2026-09-24
 * ============================================================
 */

'use strict';

module.exports = {
  /**
   * 正向迁移（基线：空操作）
   * @param {object} pool mysql2/promise 连接池
   */
  async up(pool) {
    // 基线迁移：现有表结构已存在，不执行任何 DDL
  },

  /**
   * 回滚（基线：空操作）
   * @param {object} pool mysql2/promise 连接池
   */
  async down(pool) {
    // 基线迁移：不回滚任何已有表结构
  }
};
