/**
 * babel.config.js - Babel 配置文件
 * 
 * 使用 Vue CLI 预设的 Babel 配置，包含：
 * - @babel/preset-env（ES6+ 转译）
 * - @vue/babel-preset-jsx（JSX 支持）
 */
module.exports = {
  presets: [
    '@vue/cli-plugin-babel/preset'
  ]
}
