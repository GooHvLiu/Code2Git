# 03.03-UI组件库

## 应用分类

### Vue2适配

<strong style="color:orange">PC 端后台</strong>

* Element UI（饿了么 Vue2 老牌）
  * 官网：https://element.eleme.cn/#/zh-CN

* Ant Design Vue 1.x
  * 官网：https://2x.antdv.com/

* View UI（iView）
  * 官网：https://www.iviewui.com/

* Vuetify 2
  * 官网：https://v15.vuetifyjs.com/zh-Hans/

* TDesign Vue 2
  * 官网：https://tdesign.tencent.com/vue

<strong style="color:orange">移动端</strong>

* Vant 2（有赞 Vue2 移动端）
  * 官网：https://vant-contrib.gitee.io/vant/v2/#/zh-CN/

* Mint UI（饿了么 Vue2 轻量移动端，停更）
  * 官网：https://mint-ui.github.io/#!/zh-cn

### Vue3适配

<strong style="color:orange">PC 端后台</strong>

* Element Plus（饿了么，国内最通用后台）

  * 官网：https://element-plus.org/zh-CN/

  * 对应 Vue2 旧版：Element UI

* Ant Design Vue 4.x（阿里蚂蚁，企业金融级）
  * 官网：https://www.antdv.com/

* Naive UI（图森未来，纯 TS、高性能）
  * 官网：https://www.naiveui.com/zh-CN/

* Arco Design Vue（字节跳动，大型中台）
  * 官网：https://arco.design/vue/docs/start

* TDesign Vue Next（腾讯企业轻量化后台）
  * 官网：https://tdesign.tencent.com/vue-next

* Vuetify 3（Material Design 全端）
  * 中文官网：https://vuetifyjs.com/zh-Hans/

* PrimeVue（海外老牌，多主题、海量组件）
  * 官网：https://primevue.org/

* Vuestic UI（轻量化国际风后台）
  * 官网：https://vuestic.dev/

* Buefy Next（基于 Bulma CSS 极简）
  * 官网：https://buefy.org/

<strong style="color:orange">移动端 H5</strong>

* Vant 4（有赞电商移动端首选）
  * 官网：https://vant-ui.github.io/vant/#/zh-CN

* Varlet（阿里 Material 风格移动端）
  * 官网：https://varletjs.org/#/zh-CN/home

<strong style="color:orange">跨端一套代码</strong>

* Quasar 2
  * 官网：[https://quasar.dev/](https://link.wtturl.cn/?target=https%3A%2F%2Fquasar.dev%2F&scene=im&aid=582478&lang=zh) 
  * 中文社区：[https://www.quasarchs.com/](https://link.wtturl.cn/?target=https%3A%2F%2Fwww.quasarchs.com%2F&scene=im&aid=582478&lang=zh)

## Element

#### 简单上手

##### 基本安装

```bash
npm i element-ui -S
```

##### 按需引入

###### 安装依赖

```bash
npm install babel-plugin-component -D
```

> 此依赖安装后可以按需引入使用

###### 修改配置

将babel.config.js修改为如下：

```js
// 1. presets新增["@babel/preset-env", { modules: false }]
// 2. 新增 plugins: [ [ "component", { libraryName: "element-ui", styleLibraryName: "theme-chalk" } ] ]
module.exports = {
  presets: [
    "@vue/cli-plugin-babel/preset",
    ["@babel/preset-env", { modules: false }]
  ],
  plugins: [
    [
      "component",
      {
        libraryName: "element-ui",
        styleLibraryName: "theme-chalk"
      }
    ]
  ]
};
```

###### 使用插件

在main.js中引入并使用插件：

```js
// 引入对应按需使用的插件名称
import { Button, Select, checkbox, DatePicker } from "element-ui";
// 使用对应的插件
Vue.use(Button);
Vue.use(Select);
Vue.use(checkbox);
Vue.use(DatePicker);
```

> 在如下网址查找并拷贝使用：
>
> ```text
> https://element.eleme.cn/#/zh-CN/component/button
> ```

##### 正式使用

在app.vue中正式使用：

```vue
<template>
  <div id="app">
    <h2>这是一个干净的应用</h2>
    <el-row>
      <el-button icon="el-icon-search" circle></el-button>
      <el-button type="primary" icon="el-icon-edit" circle></el-button>
      <el-button type="success" icon="el-icon-check" circle></el-button>
      <el-button type="info" icon="el-icon-message" circle></el-button>
      <el-button type="warning" icon="el-icon-star-off" circle></el-button>
      <el-button type="danger" icon="el-icon-delete" circle></el-button>
    </el-row>
    <el-checkbox
      :indeterminate="isIndeterminate"
      v-model="checkAll"
      @change="handleCheckAllChange"
      >全选</el-checkbox
    >
    <div style="margin: 15px 0"></div>
    <el-checkbox-group
      v-model="checkedCities"
      @change="handleCheckedCitiesChange"
    >
      <el-checkbox v-for="city in cities" :label="city" :key="city">{{
        city
      }}</el-checkbox>
    </el-checkbox-group>
    <div class="block">
      <span class="demonstration">默认</span>
      <el-date-picker v-model="value1" type="date" placeholder="选择日期">
      </el-date-picker>
    </div>
    <div class="block">
      <span class="demonstration">带快捷选项</span>
      <el-date-picker
        v-model="value2"
        align="right"
        type="date"
        placeholder="选择日期"
        :picker-options="pickerOptions"
      >
      </el-date-picker>
    </div>
  </div>
</template>
<script>
const cityOptions = ["上海", "北京", "广州", "深圳"];
export default {
  name: "app",
  data() {
    return {
      checkAll: false,
      checkedCities: ["上海", "北京"],
      cities: cityOptions,
      isIndeterminate: true,
      pickerOptions: {
        disabledDate(time) {
          return time.getTime() > Date.now();
        },
        shortcuts: [
          {
            text: "今天",
            onClick(picker) {
              picker.$emit("pick", new Date());
            }
          },
          {
            text: "昨天",
            onClick(picker) {
              const date = new Date();
              date.setTime(date.getTime() - 3600 * 1000 * 24);
              picker.$emit("pick", date);
            }
          },
          {
            text: "一周前",
            onClick(picker) {
              const date = new Date();
              date.setTime(date.getTime() - 3600 * 1000 * 24 * 7);
              picker.$emit("pick", date);
            }
          }
        ]
      },
      value1: "",
      value2: ""
    };
  },
  methods: {
    handleCheckAllChange(val) {
      this.checkedCities = val ? cityOptions : [];
      this.isIndeterminate = false;
    },
    handleCheckedCitiesChange(value) {
      let checkedCount = value.length;
      this.checkAll = checkedCount === this.cities.length;
      this.isIndeterminate =
        checkedCount > 0 && checkedCount < this.cities.length;
    }
  }
};
</script>
```

> 如上为最基本的使用案例，作为入门学习了解

## 触类旁通

### 使用SVG

#### 前置概要

##### 创建项目

```bash
PS F:\CodingMan\Code2Git\01-Stu\03-FrontEnd\03-Vue_2_3\02-Vue_2-Int\01-DemoCode> vue create 14-svg-using


Vue CLI v5.0.9
? Please pick a preset: Default ([Vue 2] babel, eslint)


Vue CLI v5.0.9
✨  Creating project in F:\CodingMan\Code2Git\01-Stu\03-FrontEnd\03-Vue_2_3\02-Vue_2-Int\01-DemoCode\14-svg-using.
⚙️  Installing CLI plugins. This might take a while...


added 831 packages in 18s
```

##### 安装依赖

```bash
PS F:\CodingMan\Code2Git\01-Stu\03-FrontEnd\03-Vue_2_3\02-Vue_2-Int\01-DemoCode\14-svg-using> npm install svg-sprite-loader --save-dev
npm warn deprecated urix@0.1.0: Please see https://github.com/lydell/urix#deprecated
npm warn deprecated source-map-url@0.4.1: See https://github.com/lydell/source-map-url#deprecated
npm warn deprecated resolve-url@0.2.1: https://github.com/lydell/resolve-url#deprecated
npm warn deprecated source-map-resolve@0.5.3: See https://github.com/lydell/source-map-resolve#deprecated

added 211 packages in 6s

181 packages are looking for funding
  run `npm fund` for details
```

#### 基本配置

##### 创建目录

创建`src/assets/icons`文件夹，在`icons`文件夹下创建`svg`文件夹存放本地`svg`图标。

```文本
📦src
 ┣ 📂assets
 ┃ ┣ 📂icons
 ┃ ┃ ┗ 📂svg
 ┃ ┃ ┃ ┣ 📜404.svg
 ┃ ┃ ┃ ┣ 📜agency.svg
 ┃ ┃ ┃ ┣ 📜det.svg
 ┃ ┃ ┃ ┗ 📜user.svg
 ┃ ┗ 📜logo.png
```

##### 项目配置

`vue.config.js` 中配置`svg`参数:

```js
const { defineConfig } = require('@vue/cli-service')
const path = require('path')
module.exports = defineConfig({
  transpileDependencies: true,
  chainWebpack: config => {
    // svg图标加载
    config.module
      .rule('svg')
      .exclude.add(path.join(__dirname, 'src/assets/icons/svg'))
      .end()

    config.module
      // 定义一个名叫 icons 的规则
      .rule('icons')

      // 设置 icons 的匹配正则
      .test(/\.svg$/)

      // 设置当前规则的作用目录，只在当前目录下才执行当前规则
      .include.add(path.join(__dirname, 'src/assets/icons/svg'))
      .end()

      // 指定一个名叫 svg-sprite 的 loader 配置
      .use('svg-sprite')

      // 该配置使用 svg-sprite-loader 作为处理 loader
      .loader('svg-sprite-loader')

      // 该 svg-sprite-loader 的配置
      .options({
        symbolId: 'icon-[name]'
      })
      .end()
  }
})
```

> 全新模板对应的完整`vue.config.js`代码

#### 全局引入

##### 创建文件

`src/assets/icons`文件夹创建`index.js`自动导入所有`svg`图标：

```js
const req = require.context('@/assets/icons/svg', false, /\.svg$/)
const requireAll = requireContext => {
  // requireContext.keys()数据：['./404.svg', './agency.svg', './det.svg', './user.svg']
  requireContext.keys().map(requireContext)
}
requireAll(req)

```

##### 引入文件

`main.js`中引入`src/assets/icons/index.js`:

```js
import Vue from 'vue'
import App from './App.vue'
// 引入svg使用的库文
import './assets/icons/index.js'
Vue.config.productionTip = false
new Vue({
  render: h => h(App),
}).$mount('#app')
```

##### 公用组件

创建`components/SvgIcon/index.vue`公用组件:

```vue
<template>
  <i v-if="iconFileName.indexOf('el-icon-') === 0" :class="iconFileName" />
  <svg v-else class="svg-icon" aria-hidden="true" v-on="$listeners">
    <use :xlink:href="`#icon-${iconFileName}`" rel="external nofollow" />
  </svg>
</template>

<script>
export default {
  name: "SvgIcon",
  props: {
    iconFileName: {
      type: String,
      required: true
    }
  }
};
</script>

<style scoped>
.svg-icon {
  width: 1em;
  height: 1em;
  overflow: hidden;
  vertical-align: -0.15em;
  fill: currentColor;
}
</style>
```

##### 注册组件

在`src/assets/icons/index.js`文件内增加注册：

```js
import Vue from "vue";
import SvgIcon from '@/components/SvgIcon';
// 全局注册SvgIcon
Vue.component('svg-icon', SvgIcon);

const req = require.context('@/assets/icons/svg', false, /\.svg$/)
const requireAll = requireContext => {
  // requireContext.keys()数据：['./404.svg', './agency.svg', './det.svg', './user.svg']
  requireContext.keys().map(requireContext)
}
requireAll(req)
```

#### 使用组件

在`src/components/HelloWorld.vue`中直接使用即可：

```vue
<template>
  <div class="hello">
    <h1>{{ msg }}</h1>
    <span>
      <svg-icon icon-file-name="user" />
      <svg-icon icon-file-name="404" />
    </span>
  </div>
</template>
```







