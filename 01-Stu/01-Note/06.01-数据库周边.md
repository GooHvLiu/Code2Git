# 06.01-数据库周边

## ORM工具栈

### Prisma

#### 基本定义

Prisma 是一套现代化 ORM 工具栈（替代传统 Sequelize/TypeORM），专门给 Node.js/ TypeScript 后端用，核心解决数据库操作全流程问题。

ORM：不用手写原生 SQL 语句操作数据库的工具

Prisma：其中最新、最好用的一款 ORM，只给 Node/TS 后端程序员用

Sequelize、TypeORM：老一代同类工具，Prisma 就是用来替换它们的

**原生 SQL**：自己写一堆数据库语句，容易写错、容易被黑客注入攻击、类型容易对不上报错；

**ORM 工具**：不用写 SQL，直接用 JS/TS 代码像调用函数一样增删改查数据库。

#### 基本使用

##### 安装依赖

使用node.js首先进行初始化使用npm:
```bash
PS F:\CodingMan\Code2Git\01-Stu\06.01-SqlPeriphery\01-Prisma\01-mySQL-full> npm init -y
Wrote to F:\CodingMan\Code2Git\01-Stu\06.01-SqlPeriphery\01-Prisma\01-mySQL-full\package.json:

{
  "name": "01-mysql-full",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
```

使用如下方式进行Prisma安装：

* 开发依赖：prisma、ts类型

```bash
PS F:\CodingMan\Code2Git\01-Stu\06.01-SqlPeriphery\01-Prisma\01-mySQL-full> npm i -D prisma @types/node typescript ts-node

added 116 packages in 19s

14 packages are looking for funding
  run `npm fund` for details
```

* 运行依赖：客户端、mysql适配器、环境变量

```bash
PS F:\CodingMan\Code2Git\01-Stu\06.01-SqlPeriphery\01-Prisma\01-mySQL-full> npm i @prisma/client @prisma/adapter-mariadb mariadb dotenv

added 12 packages in 4s

14 packages are looking for funding
  run `npm fund` for details
PS F:\CodingMan\Code2Git\01-Stu\06.01-SqlPeriphery\01-Prisma\01-mySQL-full> 
```

> `@prisma/adapter-mariadb`：MySQL 官方推荐 JS 驱动适配器（Prisma7 强制使用）
>
> `mariadb`：底层 mysql 驱动，兼容 MySQL8.0

##### 本地启动

* 启动 MySQL 8.0+

* 登录 MySQL 客户端创建数据库

```sql
CREATE DATABASE prisma4demo DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

* 修改 `.env` 连接字符串

```env
DATABASE_URL="mysql://root:你的数据库密码@localhost:3306/prisma_mysql_demo?schema=public"
```

> 格式说明：`mysql://账号:密码@IP:端口/数据库名`

##### 初始化源

初始化 Prisma MySQL 数据源

```bash
PS F:\CodingMan\Code2Git\01-Stu\06.01-SqlPeriphery\01-Prisma\01-mySQL-full> npx prisma init --datasource-provider mysql

Initialized Prisma in your project

  prisma/
    schema.prisma
  prisma.config.ts
  .env
  .gitignore

Next, set up your database:
  1. Configure your DATABASE_URL in prisma.config.ts
  2. Run prisma db pull to introspect your database.

Then, define your models in prisma/schema.prisma and run prisma migrate dev to apply your schema.

Learn more: https://pris.ly/getting-started
```

> 生成文件：
>
> - `prisma/schema.prisma` 数据模型定义
> - `.env` 环境变量文件
> - `prisma.config.ts` Prisma7 专属配置文件
> - `tsconfig.json` ts 配置

##### 配置变量

配置 .env 数据库连接串

```env
# .env
# 格式 mysql://账号:密码@localhost:3306/数据库名
DATABASE_URL="mysql://root:123456@127.0.0.1:3306/prisma4demo"
# 连接池配置（可选）
DATABASE_HOST=127.0.0.1
DATABASE_USER=root
DATABASE_PASS=123456
DATABASE_NAME=prisma4demo
DATABASE_PORT=3306
```

##### 配置文件

修改 prisma.config.ts（CLI 迁移工具使用，解决 P1012 报错根源）：

```ts
// prisma.config.ts
import "dotenv/config";
import { defineConfig, env } from "prisma/config";

export default defineConfig({
  // 指定schema文件位置
  schema: "prisma/schema.prisma",
  // 迁移文件存放目录
  migrations: {
    path: "prisma/migrations",
  },
  // CLI migrate/db pull 读取数据库连接（Prisma7要求，替代schema里的url）
  datasource: {
    url: env("DATABASE_URL"),
  },
});
```

#### 基本语法

##### 重写模式

重写模式文件schema.prisma文件，基本内容如下：

```文本
// prisma/schema.prisma
// 数据源：仅保留provider，删除url（Prisma7禁止在此写url）
datasource db {
  provider = "mysql"
}

// 客户端生成配置（7.x推荐指定output输出目录，不丢类型）
generator client {
  provider = "prisma-client-js"
  // 生成代码输出到项目根generated文件夹，方便导入
  output   = "../generated/prisma"
  // 无Rust二进制引擎，适配serverless/轻量化部署
  engineType = "client"
}

// 示例模型：用户表
model User {
  id        Int      @id @default(autoincrement()) // 自增主键
  username  String   @unique                      // 唯一索引
  email     String   @unique
  password  String
  nickname  String?                                // 可空字段
  status    Int      @default(1)                   // 默认值1正常
  createdAt DateTime @default(now())              // 创建时间自动当前
  updatedAt DateTime @updatedAt                    // 更新时自动刷新
  // 一对多关联：一个用户多个设备
  devices   Device[]
  // 映射真实数据库表名（可选，默认模型名大写User→user）
  @@map("sys_user")
}

// 设备表（一对多关联User）
model Device {
  id        Int    @id @default(autoincrement())
  deviceNo  String @unique
  name      String
  status    Int    @default(0)
  userId    Int
  // 关联用户外键
  user      User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  @@map("sys_device")
}

// 枚举类型（状态）
enum DeviceStatus {
  OFFLINE
  RUNNING
  ERROR
}
```

##### 迁移功能

###### 创建文件

生成迁移文件并同步到 MySQL：

```bash
# init代表迁移名称，初始化表结构
PS F:\CodingMan\Code2Git\01-Stu\06.01-SqlPeriphery\01-Prisma\01-mySQL-full> npx prisma migrate dev --name init
Loaded Prisma config from prisma.config.ts.

Prisma schema loaded from prisma\schema.prisma.
Datasource "db": MySQL database "prisma4demo" at "127.0.0.1:3306"

Applying migration `20260714135906_init`

The following migration(s) have been created and applied from new schema changes:

prisma\migrations/
  └─ 20260714135906_init/
    └─ migration.sql

Your database is now in sync with your schema.
```

> 执行流程：
>
> 1. 对比 schema 与数据库差异，生成 `prisma/migrations/时间_init/migration.sql`
> 2. 自动执行 SQL，在 prisma4demo库创建 `sys_user`、`sys_device` 两张表
> 3. 自动执行 `prisma generate` 生成带类型的客户端代码

###### 生成文件

生成客户端，后续改 schema 后执行:

```bash
PS F:\CodingMan\Code2Git\01-Stu\06.01-SqlPeriphery\01-Prisma\01-mySQL-full> npx prisma generate
Loaded Prisma config from prisma.config.ts.

Prisma schema loaded from prisma\schema.prisma.

✔ Generated Prisma Client (v7.8.0) to .\generated\prisma in 101ms

Start by importing your Prisma Client (See: https://pris.ly/d/importing-client)
```

> 作用：根据 schema 生成 TS 类型，代码自动补全、类型校验。

##### 封装功能

封装全局 PrismaClient（核心：必须传入 adapter 适配器，Prisma7 强制），新建 `src/db/prisma.ts`，全局单例客户端（避免重复创建连接）：

```ts
// src/db/prisma.ts
import "dotenv/config";
import { PrismaClient } from "../../generated/prisma/client";
import { PrismaMariaDb } from "@prisma/adapter-mariadb";

// 构建mysql适配器
const adapter = new PrismaMariaDb({
  host: process.env.DATABASE_HOST,
  user: process.env.DATABASE_USER,
  password: process.env.DATABASE_PASS,
  database: process.env.DATABASE_NAME,
  port: Number(process.env.DATABASE_PORT),
  // 连接池数量
  connectionLimit: 10,
});

// 全局单例，全局只创建一次客户端
const globalForPrisma = globalThis as unknown as { prisma: PrismaClient };

export const prisma = globalForPrisma.prisma || new PrismaClient({ adapter });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;

// 程序退出关闭数据库连接
export default prisma;
```

#### 使用功能

##### 基本功能

基础 CRUD 实战（增删改查完整示例），新建 `src/script.ts` 测试脚本：

```ts
// src/script.ts
import prisma from "./db/prisma";

async function main() {
  // 1. 创建用户 + 同时创建2台设备（嵌套写入，单事务）
  const user = await prisma.user.create({
    data: {
      username: "zhangsan",
      email: "zhangsan@test.com",
      password: "123456",
      nickname: "张三",
      devices: {
        create: [
          { deviceNo: "DEV001", name: "数控车床", status: 1 },
          { deviceNo: "DEV002", name: "检测仪", status: 0 },
        ],
      },
    },
    // 查询时带出关联设备
    include: { devices: true },
  });
  console.log("创建用户：", user);

  // 2. 查询单条数据（根据id）
  const findUser = await prisma.user.findUnique({
    where: { id: user.id },
    include: { devices: true },
  });

  // 3. 条件查询多条 + 分页 + 排序
  const userList = await prisma.user.findMany({
    skip: 0, // 跳过多少条
    take: 10, // 每页条数
    where: { status: 1 },
    orderBy: { createdAt: "desc" },
  });

  // 4. 更新数据
  await prisma.user.update({
    where: { id: user.id },
    data: { nickname: "张三工程师" },
  });

  // 5. 批量更新（无返回单条对象，适合批量操作）
  await prisma.device.updateMany({
    where: { userId: user.id },
    data: { status: 1 },
  });

  // 6. 删除单条
  await prisma.device.delete({ where: { deviceNo: "DEV002" } });

  // 7. 批量删除
  await prisma.device.deleteMany({ where: { status: 0 } });
}

main()
  .catch((e) => console.error(e))
  .finally(async () => await prisma.$disconnect());
```

##### 脚本运行

```bash
npm run script
```



